<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ll_row_img_header"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingBottom="@dimen/px20"
    >

    <ImageView
        android:id="@+id/row_img_header"
        android:layout_width="@dimen/px50"
        android:layout_height="@dimen/px50"
        android:layout_marginEnd="@dimen/px20"
        android:layout_gravity="center"
        android:src="@drawable/waterfall_default_title" />

    <androidx.leanback.widget.RowHeaderView
        android:id="@+id/row_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/app_name"
        android:importantForAccessibility="no"
        android:textColor="@color/colorWhite"
        android:textSize="@dimen/px48" />

</LinearLayout>